/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { IbexcelreportService } from './ibexcelreport.service';

describe('Service: Ibexcelreport', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IbexcelreportService]
    });
  });

  it('should ...', inject([IbexcelreportService], (service: IbexcelreportService) => {
    expect(service).toBeTruthy();
  }));
});
